# Project environment policy (checked in)
# Keep non-secret defaults here; put secrets in .envrc.local.

# Load local secrets/config if present.
[[ -f .envrc.local ]] && source_env .envrc.local

# If this project uses Nix flakes, uncomment:
# use flake
