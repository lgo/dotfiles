#!/usr/bin/env bash
set -euo pipefail

dotfiles_root="$(cd "$(dirname "$0")/.." && pwd)"
cd "$dotfiles_root"

echo "Running darwin-rebuild build for nix changes..."
darwin-rebuild build --flake ./nixos#sprout
