# Docs

- `architecture.md`: high-level structure and module boundaries.
- `hosts.md`: active host model and naming.
- `symlinks.md`: out-of-store symlink rationale and rules.
- `homebrew-boundaries.md`: ownership split between Nix and Homebrew.
- `direnv.md`: using `direnv` + `nix-direnv` with `use flake`.
- `onboarding.md`: first-run machine checklist and validation flow.
